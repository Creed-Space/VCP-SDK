{
  "name": "vcp-sdk-validation",
  "private": true,
  "version": "0.0.0",
  "description": "Pinned repository and JSON Schema validation tooling for VCP-SDK",
  "engines": {
    "node": ">=22.12.0 <23 || >=24.0.0 <25"
  },
  "scripts": {
    "validate:schemas": "node scripts/check_strict_schemas.mjs"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1"
  }
}
